name: widget_collapser
description: A Flutter package that collapses toolbar children into an overflow widget as space shrinks.
version: 0.0.1
repository: https://github.com/patrick/widget_collapser
homepage: https://github.com/patrick/widget_collapser
license: MIT

environment:
  sdk: ^3.12.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
